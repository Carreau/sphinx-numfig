============
Installation
============

At the command line::

    $ easy_install sphinx-numfig

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv sphinx-numfig
    $ pip install sphinx-numfig